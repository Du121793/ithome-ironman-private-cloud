# VM、Storage 與服務配置

## VMID 分段

| 範圍 | 類型 |
|---:|---|
| 100～109 | Nested PVE |
| 110～119 | Firewall／Network Appliance |
| 200～209 | Proxy／Load Balancer |
| 210～219 | Application／Web Backend |
| 220～229 | PostgreSQL／Database |
| 230～239 | Bastion |
| 240～249 | 測試 Client |
| 250～259 | Monitoring |
| 260～269 | Backup |
| 270～279 | 暫時還原 VM |
| 900～999 | 可丟棄測試 VM |
| 9000 以上 | Template；本系列使用 9001 |

Cluster 內 VMID 全域唯一，VM Migration 後不更換 VMID。

VMID `100` 保留，不建立正式 VM；101～103 才是三台 Nested PVE。

## L0 VM

| VMID | 名稱 | 初始位置 | Storage | 功能 |
|---:|---|---|---|---|
| 101 | `pve01` | pve-l0 | L0 `local-lvm` | L1 PVE 節點 1 |
| 102 | `pve02` | pve-l0 | L0 `local-lvm` | L1 PVE 節點 2 |
| 103 | `pve03` | pve-l0 | L0 `local-lvm` | L1 PVE 節點 3 |
| 110 | `fw01` | pve-l0 | L0 `local-lvm` | OPNsense WAN、VLAN Gateway、Firewall、NAT、DNS、OpenVPN、IDS／IPS |

## L1 正式與測試 VM

| VMID | 名稱 | 初始節點 | vCPU | RAM | 磁碟 | Storage | 服務／用途 |
|---:|---|---|---:|---:|---|---|---|
| 201 | `proxy01` | pve01 | 2 | 1GB | 16GB | local-lvm | Nginx、HAProxy、Keepalived |
| 202 | `proxy02` | pve02 | 2 | 1GB | 16GB | local-lvm | Nginx、HAProxy、Keepalived |
| 211 | `app01` | pve02 | 1 | 1GB | 12GB | ceph-vm | Flask、psycopg2 Web Backend 1 |
| 212 | `app02` | pve03 | 1 | 1GB | 12GB | ceph-vm | Flask、psycopg2 Web Backend 2 |
| 221 | `pg01` | pve01 | 2 | 2GB | System 24GB 以上＋Data 16GB | local-lvm | PostgreSQL 18、Patroni、etcd01 |
| 222 | `pg02` | pve02 | 2 | 2GB | System 24GB 以上＋Data 16GB | local-lvm | PostgreSQL 18、Patroni、etcd02 |
| 223 | `pg03` | pve03 | 2 | 2GB | System 24GB 以上＋Data 16GB | local-lvm | PostgreSQL 18、Patroni、etcd03 |
| 224 | `ca01` | pve01 | 1 | 512MB | 8GB | local-lvm | `step-ca` Lab Internal CA；不承載 Root CA 長期線上用途 |
| 231 | `jump01` | pve03 | 1 | 1GB | 8GB | local-lvm | OpenSSH Bastion；只有 VLAN 50 NIC |
| 241 | `client01` | pve03 | 1 | 1GB | 8GB | local-lvm | curl、psql、jq 與連續 Web／SQL 測試 |
| 251 | `monitor01` | pve02 | 2 | 2GB | 24GB | local-lvm | Prometheus、Grafana、Alertmanager、Blackbox Exporter |
| 261 | `backup01` | pve01 | 2 | 2GB | System Disk＋Repository 32GB | local-lvm | pgBackRest Repository |
| 271 | `pg-restore01` | 臨時放置 | 2 | 2GB | Data 16GB | local-lvm | Day 28 隔離 PITR；不加入 Patroni／etcd |
| 902 | `cloudinit-test01` | 可遷移 | 2 | 2GB | 依 Template | ceph-vm | Day 06／09 可丟棄 Cloud-Init、Migration、PVE HA 測試 |
| 9001 | `debian-cloud-template` | pve01 | 2 | 2GB | Debian Cloud Image | local-lvm | Debian 13 Template，不承載正式服務 |

`app01` 在 Day 16 先建立網路與 SSH 基線，Day 25 才安裝 Web Backend；`monitor01` 也在 Day 16 先建立，Day 29 才安裝監控，不得重新 Clone 相同 VMID。

## 每台 PVE 最終服務分布

| PVE 節點 | VM | VM 內服務 |
|---|---|---|
| pve01 | proxy01 | Nginx、HAProxy、Keepalived |
| pve01 | pg01 | PostgreSQL、Patroni、etcd01 |
| pve01 | ca01 | step-ca Online Intermediate CA |
| pve01 | backup01 | pgBackRest Repository |
| pve02 | proxy02 | Nginx、HAProxy、Keepalived |
| pve02 | app01 | Flask Web Backend、psycopg2 |
| pve02 | pg02 | PostgreSQL、Patroni、etcd02 |
| pve02 | monitor01 | Prometheus、Grafana、Alertmanager、Exporters |
| pve03 | app02 | Flask Web Backend、psycopg2 |
| pve03 | pg03 | PostgreSQL、Patroni、etcd03 |
| pve03 | jump01 | OpenSSH Bastion |
| pve03 | client01 | Web／SQL 故障測試工具 |

初始位置是部署與故障分散基準，不代表所有 VM 都已加入 PVE HA。只有磁碟可由其他節點存取、CPU 相容且另行加入 HA Resource 的 VM，才具備由 PVE 在其他節點恢復的條件。
