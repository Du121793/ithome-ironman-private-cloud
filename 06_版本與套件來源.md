# 版本、映像與套件來源

| 元件 | 系列固定值／來源 | 記錄方式 |
|---|---|---|
| Proxmox VE ISO | `proxmox-ve_9.2-1.iso`，Proxmox 官方下載頁 | 保存 ISO 檔名與 SHA256 |
| PVE Base | Debian 13 `trixie` | `pveversion -v`、`cat /etc/os-release` |
| PVE Repository | Proxmox VE `trixie` no-subscription | 保存 `.sources`／`.list` 內容 |
| Ceph | 19.2 Squid；`pveceph install --version squid` | 三節點保存 `ceph --version`；不要使用會選到其他大版本的裸 `pveceph install` |
| Ceph Repository | `http://download.proxmox.com/debian/ceph-squid`，`trixie no-subscription` | 三節點保持一致 |
| Debian Guest | Debian 13 Trixie Generic Cloud AMD64 QCOW2 | 保存下載 URL、檔案大小、Checksum；匯入後建立 VMID 9001 Template |
| OPNsense | `OPNsense-26.7-dvd-amd64.iso` | 保存 ISO、SHA256、Signature；更新後小版本以錄製日實機為準 |
| PostgreSQL | 18，PGDG Repository | `psql --version`、`apt-cache policy postgresql-18` |
| etcd | Debian 13 `etcd-server`、`etcd-client` | `etcd --version`、`etcdctl version`；三節點相同 |
| step-ca／step-cli | Smallstep 官方 Debian Repository | ca01 保存 `step-ca version`；pg01～03 保存 `step version`，錄製時記錄實際小版本 |
| pgBackRest | PGDG Repository 的同一版本 | backup01、pg01～03 與 restore VM 保存 `pgbackrest version`／`apt-cache policy pgbackrest` |
| Prometheus／Alertmanager／Exporters | Debian 13 Repository | 保存 `apt-cache policy` 與 Service 狀態 |
| Grafana OSS | Grafana 官方 APT Repository | 保存 `grafana-server -v` 與 Repository 設定 |

Ceph 固定 Squid 是為了與已安裝的 19.2 套件及錄製素材一致，不代表新的正式環境永遠應停留在 Squid。正式升級必須另做 PVE／Ceph 相容性與逐步升級評估。

DHCP 位址、OPNsense 更新後 Patch Version、PostgreSQL 小版本、etcd Package Version、pgBackRest 與監控套件版本都可能隨錄製日改變。表格只固定大版本或來源；每次部署要把實機輸出保存到當日證據，不臆測不存在的小版本。
