# 硬體、儲存與資源配置

## 實體示範主機

| 主機 | CPU | RAM | 實體儲存 | 用途 |
|---|---:|---:|---:|---|
| `pve-l0` | 12 Core | 48GB | 500GB 單碟 | 承載三台 Nested PVE 與 `fw01` |

這是單機巢狀 Lab。三台 PVE、三顆虛擬 Ceph OSD、OPNsense 與所有服務最後仍共用同一台主機、同一顆實體磁碟、同一份電力與 CPU，不能作為正式環境跨實體故障域的 HA 證明。

## L0 主要 VM 資源

| VMID | 名稱 | vCPU | RAM | 磁碟 | 備註 |
|---:|---|---:|---:|---|---|
| 101 | `pve01` | 4 | 12GB | 80GiB System＋40GiB Ceph OSD | Nested PVE |
| 102 | `pve02` | 4 | 12GB | 80GiB System＋40GiB Ceph OSD | Nested PVE |
| 103 | `pve03` | 4 | 12GB | 80GiB System＋40GiB Ceph OSD | Nested PVE |
| 110 | `fw01` | 2 | 3072MiB | 16GB | OPNsense；不啟用 Ballooning |

三台 Nested PVE 的 CPU Type 使用 `host`。由於它們來自同一顆 L0 CPU，可用於本 Lab 的 Live Migration 實驗；這不代表不同世代或不同廠牌的實體 CPU 也一定相容。

## Ceph 配置

| 項目 | 本 Lab 設定 |
|---|---|
| Ceph 網路 | `10.77.70.0/24`，無 Gateway |
| OSD | 每個 PVE 節點一顆 40GiB 虛擬磁碟，共三顆 |
| MON | pve01、pve02、pve03 |
| MGR | 至少 pve01、pve02 |
| RBD Pool | `ceph-vm`，Size 3、Min Size 2、PG Autoscaler `on` |
| OSD Memory Target | 每個 OSD 2GiB |
| CephFS MDS | pve01 Active／Standby 與 pve02 Standby／Active；不在 pve03 建第三個 MDS |
| CephFS | `cephfs`，只保存共享 ISO；Data 與 Metadata Pool 各以 32 PG 為目前目標並啟用 Autoscaler |
| CephFS Quota | 選用的 5GiB 邏輯 Quota，不是預留 5GiB 實體空間 |

`ceph-vm` 目前承載 VMID 902 測試 VM及 `app01`、`app02` 的小型示範工作負載。PostgreSQL、監控與備份資料預設留在各節點 `local-lvm`，避免有限的三副本空間被持續增長的資料占滿。

## 容量操作原則

- `ca01` 使用 1 vCPU、512MB RAM 與 8GB `local-lvm`，只承載小型 `step-ca`。若實測發生 OOM，再將 RAM 調整為 1GB。
- 服務可以在低負載下同時保持開機，但 Ceph Recovery、備份、PostgreSQL 壓力測試與大量更新必須錯開。
- Thin Provisioning 的虛擬容量不等於實體可用容量；每次新增磁碟前同時確認 L0、L1 Thin Pool 與 Guest Filesystem。
- `backup01` Repository Disk 目前規劃 32GB，但建立前仍須確認剩餘空間；低於 20% 時先停止擴充。
- CephFS 寫滿 5GiB 時，三副本最多約使用 15GiB Raw Capacity，另有 Metadata 與系統 Overhead。
- Lab 不使用 ZFS 作為主要底層，避免單顆 500GB 磁碟與有限 RAM 再疊加不必要的容量壓力。
